# ==============================================================
# NGINX PROXY MANAGER - ПРИМЕР КОНФИГУРАЦИИ
# ==============================================================
# Этот файл содержит примеры переменных окружения для локальной
# разработки и тестирования. При деплое через GitHub Actions
# эти переменные настраиваются через секреты GitHub.
# ==============================================================

# -----------------------------
# ОСНОВНЫЕ НАСТРОЙКИ
# -----------------------------

# Список доменов и портов для проксирования
# Формат: домен:порт,домен:порт
# Пример: example.com:80,api.example.com:3000,admin.example.com:8080
DOMAINS=example.com:80,api.example.com:3000,admin.example.com:8080

# IP-адрес сервера, на котором запущен прокси
# Для локальной разработки можно использовать 127.0.0.1
SERVER_IP=123.45.67.89

# Имя проекта для Docker Compose
# Используется как префикс для контейнеров, сетей и томов
COMPOSE_PROJECT_NAME=nginx-proxy

# -----------------------------
# НАСТРОЙКИ SSL
# -----------------------------

# Включить получение и настройку SSL-сертификатов Let's Encrypt
# true - включено, false - отключено
ENABLE_SSL=false

# Email для Let's Encrypt (обязательно при ENABLE_SSL=true)
# Используется для уведомлений об истечении сертификатов
EMAIL_FOR_SSL=your-email@example.com

# Email для системных уведомлений
NOTIFY_EMAIL=your-email@example.com

# -----------------------------
# НАСТРОЙКИ ДЛЯ GITHUB ACTIONS
# -----------------------------
# Следующие переменные нужны только при деплое через GitHub Actions
# и настраиваются через секреты GitHub (не в этом файле):

# SERVER_USER - пользователь SSH для подключения к серверу
# Пример: root, ubuntu, admin

# SSH_PRIVATE_KEY - приватный ключ для SSH-подключения к серверу
# Содержимое файла ~/.ssh/id_rsa или другого приватного ключа 